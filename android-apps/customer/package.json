{
  "name": "sevk-customer",
  "version": "1.0.0",
  "description": "SEVK Customer App",
  "scripts": {
    "build": "echo 'Web assets copied by CI'",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
